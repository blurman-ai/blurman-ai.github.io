source "https://rubygems.org"

# Pinned to what Ruby 2.5 (this machine's system Ruby) can still build.
# Bump these once the machine moves to a newer Ruby.
gem "jekyll", "4.2.2"
gem "minima", "2.5.1"

group :jekyll_plugins do
  gem "jekyll-feed", "0.17.0"
end

gem "webrick"
